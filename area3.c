#include<stdio.h>
int main()
{
	int radius = 3;
	float pi = 3.14;
	printf("the area of the circle is %f\n", pi * radius * radius);
	int height = 5;
	printf("the area of this cylinder is %f\n", pi * radius * radius * height);
	return 0;
}
