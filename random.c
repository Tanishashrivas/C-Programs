#include<stdio.h>
#include<conio.h>

int main()
{
	int a = 5;
	int b = 8;
	int c = 2;
	int multi = (a * b * c);
	float pi = 3.14;
	float w = 1.2;
    float x = (pi * w);
	printf("multiplication of a and b and c is %d", multi);
	printf(" pi multiplied by w gives %f", x);
	return 0;
}
